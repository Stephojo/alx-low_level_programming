iinclude "main.h"

/**
 *  _strcmp - compares the value of two strings
 *  @s1: first string
 *  @s2: second string
 *  Return: the differece between the strings value
 */

int _strcmp(char *s1, char *s2)
{
	while (*s1 != *s2 && *si == *s2)
	{
	}
	return (*s1 - *s2);
}
