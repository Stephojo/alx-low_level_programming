#include "main.h"

/**
 * mul - fint the multiplication of two integers
 *
 * Return: give the multiplication of the integers
 * @a: first parameter
 * @b: second parameter0
 */

int mul(int a, int b)

{
	return (a * b);
}

