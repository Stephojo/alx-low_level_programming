#include <stdio.h>
/**
 * main - the starting point
 *
 * Return: gives 0 if (success)
 */

int main(void)
{
	char d = 'a';

	while (d <= 'z')

	{
		putchar(d);
		d++;
	}
	putchar('\n');
	return (0);
}

